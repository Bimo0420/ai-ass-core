# Справочник параметров моделей Qwen3 (Ollama)

В этом файле собраны настройки (Modelfile) всех версий модели Qwen3, установленных в вашей системе.

---

## 🚀 qwen3:30b-notool (РЕКОМЕНДУЕМАЯ)
**Особенности**: Отключен режим "рассуждения" (thinking), оптимизирована для быстрого вызова инструментов.

```modelfile
$(cat model_specs_notool.txt)
```

---

## 💎 qwen3:30b-clean
**Особенности**: Чистая модель без системного промпта. Идеальна, когда вы хотите полностью контролировать агентa через n8n.

```modelfile
$(cat model_specs_clean.txt)
```

---

## 🤖 qwen3:30b-rag
**Особенности**: Системный промпт вшит в модель. Может конфликтовать с настройками n8n.

```modelfile
$(cat model_specs_rag.txt)
```

---

## 📦 qwen3:30b (Base)
**Особенности**: Стандартная модель без изменений.

```modelfile
$(cat model_specs_base.txt)
```

---

## 🛠 Как изменить параметры любой модели?

Чтобы создать свою версию или обновить текущую:
1. Отредактируйте нужный `.Modelfile` в папке `/tmp/`.
2. Выполните команду:
   ```bash
   docker exec ollama ollama create <название_модели> -f /tmp/<ваш_файл>.Modelfile
   ```
