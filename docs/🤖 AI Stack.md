# 🤖 AI Stack - Полная инфраструктура для работы с LLM

Комплексное решение для развертывания и управления AI-инфраструктурой на базе Docker, включающее автоматизацию (n8n), локальные LLM (Ollama), веб-интерфейс для чатов (Open WebUI), векторную базу данных (Supabase), мониторинг (Grafana + Prometheus) и трейсинг (Langfuse).

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Docker](https://img.shields.io/badge/docker-required-blue.svg)](https://www.docker.com/)
[![Docker Compose](https://img.shields.io/badge/docker--compose-v2.20+-blue.svg)](https://docs.docker.com/compose/)

---

## 📋 Содержание

- [Архитектура](#-архитектура)
- [Компоненты стека](#-компоненты-стека)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Структура проекта](#-структура-проекта)
- [Доступ к сервисам](#-доступ-к-сервисам)
- [Управление стеком](#-управление-стеком)
- [Конфигурация](#-конфигурация)
- [Мониторинг и трейсинг](#-мониторинг-и-трейсинг)
- [Резервное копирование](#-резервное-копирование)
- [Устранение неполадок](#-устранение-неполадок)
- [Безопасность](#-безопасность)
- [Дополнительные ресурсы](#-дополнительные-ресурсы)

---

## 🏗 Архитектура

\`\`\`
┌─────────────────────────────────────────────────────────────┐
│                      Traefik (Reverse Proxy)                 │
│                    SSL/TLS + Load Balancing                  │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────▼────────┐   ┌────────▼────────┐   ┌───────▼────────┐
│      n8n       │   │  Open WebUI     │   │   Langfuse     │
│  (Automation)  │   │   (Chat UI)     │   │  (Tracing)     │
└───────┬────────┘   └────────┬────────┘   └───────┬────────┘
        │                     │                     │
        │                     │                     │
┌───────▼────────┐   ┌────────▼────────┐   ┌───────▼────────┐
│    Ollama      │   │   PostgreSQL    │   │    Redis       │
│     (LLM)      │   │   (Database)    │   │    (Cache)     │
└────────────────┘   └─────────────────┘   └────────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────▼────────┐   ┌────────▼────────┐   ┌───────▼────────┐
│   Supabase     │   │  LlamaIndex     │   │   Grafana      │
│  (Vector DB)   │   │     (RAG)       │   │ (Monitoring)   │
└────────────────┘   └─────────────────┘   └────────────────┘
\`\`\`

---

## 🧩 Компоненты стека

### Основные сервисы

| Сервис | Описание | Версия | Порт |
|--------|----------|--------|------|
| **Traefik** | Reverse proxy с автоматическим SSL | v3.1 | 80, 443 |
| **n8n** | Платформа автоматизации workflow | latest | 5678 |
| **Ollama** | Локальные LLM модели | latest | 11434 |
| **Open WebUI** | Веб-интерфейс для чатов с LLM | main | 8080 |
| **PostgreSQL** | Основная база данных | 15.6.1.117 | 5432 |
| **Redis** | Кэш и очереди | 7-alpine | 6379 |

### Дополнительные сервисы

| Сервис | Описание | Версия |
|--------|----------|--------|
| **Supabase** | Векторная БД + Auth + Storage | latest |
| **LlamaIndex** | Фреймворк для RAG | custom |
| **Docling** | Парсинг документов | latest |
| **Langfuse** | Трейсинг и мониторинг LLM | latest |
| **ClickHouse** | Аналитическая БД для Langfuse | 24.3 |
| **MinIO** | S3-совместимое хранилище | 2024-01-31 |
| **Grafana** | Визуализация метрик | 11.0.0 |
| **Prometheus** | Сбор метрик | v2.52.0 |
| **Node Exporter** | Метрики системы | v1.9.0 |
| **cAdvisor** | Метрики контейнеров | latest |

---

## 💻 Требования

### Минимальные требования

- **OS**: Linux (Ubuntu 22.04+ / Debian 12+)
- **CPU**: 4 ядра
- **RAM**: 8 GB
- **Disk**: 50 GB SSD
- **Docker**: 24.0+
- **Docker Compose**: 2.20+

### Рекомендуемые требования

- **CPU**: 8+ ядер
- **RAM**: 16+ GB
- **Disk**: 100+ GB NVMe SSD
- **GPU**: NVIDIA GPU с 8+ GB VRAM (опционально)
- **Network**: 1 Gbps

### Дополнительные требования

- Доменное имя с доступом к DNS
- Cloudflare аккаунт (для SSL сертификатов)
- SSH доступ к серверу

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

\`\`\`bash
git clone https://github.com/Bimo0420/ai-ass-core.git
cd ai-ass-core
\`\`\`

### 2. Настройка переменных окружения

\`\`\`bash
# Копирование примера
cp .env.example .env

# Редактирование основных параметров
nano .env
\`\`\`

**Обязательно измените:**
- \`DOMAIN\` - ваш домен
- \`CF_API_EMAIL\` - email Cloudflare
- \`CF_DNS_API_TOKEN\` - API токен Cloudflare
- \`ACME_EMAIL\` - email для Let's Encrypt
- Все пароли (POSTGRES_PASSWORD, GF_SECURITY_ADMIN_PASSWORD, и т.д.)

### 3. Генерация секретных ключей

\`\`\`bash
# Автоматическая генерация всех ключей
make env-generate
\`\`\`

Или вручную:
\`\`\`bash
chmod +x scripts/setup/generate-secrets.sh
./scripts/setup/generate-secrets.sh
\`\`\`

### 4. Настройка DNS

Создайте A-записи в Cloudflare, указывающие на IP вашего сервера:

\`\`\`
A  *.your-domain.com   -> your-server-ip
\`\`\`

Или создайте отдельные записи для каждого сервиса:
\`\`\`
n8n, openwebui, ollama, langfuse, grafana, prometheus, 
supabase, studio, minio, traefik, docling, llamaindex
\`\`\`

### 5. Запуск стека

\`\`\`bash
# Для CPU версии
make up-cpu

# Для GPU версии (требуется NVIDIA Docker)
make up-gpu

# Или поэтапно
make up-base        # Сначала базовые сервисы
make up-apps        # Затем приложения
make up-monitoring  # И мониторинг
\`\`\`

### 6. Загрузка моделей Ollama

\`\`\`bash
# Автоматическая загрузка базовых моделей
make ollama-pull

# Или вручную
docker exec -it ollama ollama pull llama3.2
docker exec -it ollama ollama pull mistral
docker exec -it ollama ollama pull nomic-embed-text
\`\`\`

### 7. Проверка статуса

\`\`\`bash
# Статус контейнеров
make ps

# Логи всех сервисов
make logs

# Проверка доступности
make health
\`\`\`

---

## 📁 Структура проекта

\`\`\`
ai-ass-core/
├── docker-compose.base.yml          # Базовые сервисы (Traefik, PostgreSQL, Redis)
├── docker-compose.cpu.yml           # LLM сервисы для CPU
├── docker-compose.gpu.yml           # LLM сервисы для GPU
├── docker-compose.monitoring.yml    # Мониторинг и трейсинг
├── Makefile                         # Команды управления
├── .env                             # Переменные окружения (не коммитить!)
├── .env.example                     # Шаблон переменных
├── .gitignore
├── README.md
│
├── configs/                         # Конфигурации
│   ├── base/
│   │   ├── traefik/
│   │   │   ├── traefik.yml
│   │   │   └── config/
│   │   ├── supabase/
│   │   │   └── kong.yml
│   │   └── prometheus/
│   │       └── prometheus.yml
│   ├── development/
│   ├── staging/
│   └── production/
│
├── apps/                            # Пользовательские приложения
│   ├── llamaindex/
│   │   ├── src/
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   └── docling/
│
├── scripts/                         # Утилиты и автоматизация
│   ├── setup/
│   │   ├── generate-secrets.sh
│   │   └── init.sh
│   ├── backup/
│   │   ├── backup.sh
│   │   └── restore.sh
│   └── health-check.sh
│
├── docs/                            # Документация
│   ├── installation.md
│   ├── configuration.md
│   ├── troubleshooting.md
│   └── architecture/
│
├── volumes/                         # Persistent data (в .gitignore)
│   ├── n8n/
│   ├── postgres/
│   ├── ollama/
│   └── grafana/
│
└── shared/                          # Общие данные между сервисами
\`\`\`

---

## 🌐 Доступ к сервисам

После запуска все сервисы доступны по HTTPS:

| Сервис | URL | Credentials |
|--------|-----|-------------|
| **n8n** | \`https://n8n.\${DOMAIN}\` | Создать при первом входе |
| **Open WebUI** | \`https://openwebui.\${DOMAIN}\` | Создать при первом входе |
| **Langfuse** | \`https://langfuse.\${DOMAIN}\` | Email: \`\${ACME_EMAIL}\` <br> Password: \`\${LANGFUSE_INIT_USER_PASSWORD}\` |
| **Grafana** | \`https://grafana.\${DOMAIN}\` | User: \`admin\` <br> Password: \`\${GF_SECURITY_ADMIN_PASSWORD}\` |
| **Prometheus** | \`https://prometheus.\${DOMAIN}\` | - |
| **Supabase Studio** | \`https://studio.\${DOMAIN}\` | - |
| **Supabase API** | \`https://supabase.\${DOMAIN}\` | API Keys в \`.env\` |
| **MinIO Console** | \`https://minio.\${DOMAIN}\` | User: \`\${MINIO_ROOT_USER}\` <br> Password: \`\${MINIO_ROOT_PASSWORD}\` |
| **Traefik Dashboard** | \`https://traefik.\${DOMAIN}\` | - |
| **LlamaIndex API** | \`https://llamaindex.\${DOMAIN}\` | - |
| **Docling** | \`https://docling.\${DOMAIN}\` | - |
| **Ollama API** | \`https://ollama.\${DOMAIN}\` | - |

---

## 🎮 Управление стеком

### Основные команды

\`\`\`bash
make help              # Показать все доступные команды
make up-cpu            # Запустить полный стек (CPU)
make up-gpu            # Запустить полный стек (GPU)
make down              # Остановить все сервисы
make restart           # Перезапустить все сервисы
make logs              # Показать логи всех сервисов
make ps                # Статус контейнеров
\`\`\`

### Частичное управление

\`\`\`bash
make up-base           # Только базовые сервисы
make up-apps           # Только приложения
make up-monitoring     # Только мониторинг
make restart-base      # Перезапустить базу
make restart-apps      # Перезапустить приложения
make logs-monitoring   # Логи мониторинга
\`\`\`

### Ollama

\`\`\`bash
make ollama-pull       # Загрузить базовые модели
make ollama-list       # Список установленных моделей

# Или напрямую
docker exec -it ollama ollama pull <model-name>
docker exec -it ollama ollama list
docker exec -it ollama ollama run llama3.2 "Привет!"
\`\`\`

### Утилиты

\`\`\`bash
make health            # Проверка здоровья сервисов
make stats             # Статистика использования ресурсов
make backup            # Создать резервную копию
make restore           # Восстановить из копии
make clean             # Очистить неиспользуемые ресурсы
make pull              # Обновить все образы
\`\`\`

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

Основные категории переменных:

\`\`\`env
# Домен и SSL
DOMAIN=your-domain.com
ACME_EMAIL=your-email@example.com

# Cloudflare
CF_API_EMAIL=your-email@example.com
CF_DNS_API_TOKEN=your-cloudflare-token

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=strong-password
POSTGRES_HOST=postgres
POSTGRES_DB=postgres

# Секретные ключи (генерируются автоматически)
N8N_ENCRYPTION_KEY=...
WEBUI_SECRET_KEY=...
JWT_SECRET=...
NEXTAUTH_SECRET=...
SALT=...
\`\`\`

Полный список переменных см. в [.env.example](.env.example)

### Модульная архитектура Compose

Проект использует модульный подход с несколькими compose файлами:

- **docker-compose.base.yml** - Базовая инфраструктура (Traefik, PostgreSQL, Redis)
- **docker-compose.cpu.yml** - LLM сервисы для CPU
- **docker-compose.gpu.yml** - LLM сервисы с GPU
- **docker-compose.monitoring.yml** - Мониторинг и трейсинг

Преимущества:
- Гибкий запуск только нужных компонентов
- Легкое переключение между CPU и GPU
- Изоляция окружений
- Простота обслуживания

---

## 📊 Мониторинг и трейсинг

### Prometheus

Сбор метрик:
- Системные метрики (Node Exporter)
- Метрики контейнеров (cAdvisor)
- Метрики приложений (n8n, PostgreSQL)

URL: \`https://prometheus.\${DOMAIN}\`

### Grafana

Визуализация метрик и дашборды:

**Рекомендуемые дашборды для импорта:**
1. **Node Exporter Full** (ID: 1860) - системные метрики
2. **Docker Container & Host Metrics** (ID: 179) - метрики контейнеров
3. **PostgreSQL Database** (ID: 9628) - метрики БД

**Импорт дашбордов:**
1. Откройте Grafana → Dashboards → Import
2. Введите ID дашборда
3. Выберите Prometheus data source
4. Нажмите Import

URL: \`https://grafana.\${DOMAIN}\`

### Langfuse

Трейсинг LLM запросов и анализ:
- История всех запросов к LLM
- Использование токенов
- Время ответа
- Стоимость запросов
- Промпты и ответы

**Получение API ключей:**
1. Войдите в Langfuse
2. Settings → API Keys → Create new key
3. Скопируйте \`LANGFUSE_PUBLIC_KEY\` и \`LANGFUSE_SECRET_KEY\`
4. Добавьте в \`.env\`
5. Перезапустите pipelines: \`docker compose restart pipelines\`

URL: \`https://langfuse.\${DOMAIN}\`

---

## 💾 Резервное копирование

### Автоматическое резервное копирование

\`\`\`bash
# Создание резервной копии
make backup

# Или напрямую
./scripts/backup/backup.sh
\`\`\`

Скрипт создаст в директории \`export/\`:
- SQL дампы всех баз данных PostgreSQL
- Архивы Docker volumes
- Конфигурационные файлы
- Файл с метаданными

### Восстановление

\`\`\`bash
# Восстановление из резервной копии
make restore

# Или напрямую
./scripts/backup/restore.sh
\`\`\`

### Настройка автоматического бэкапа

\`\`\`bash
# Добавление cron задачи (ежедневно в 2:00)
crontab -e

# Добавьте строку:
0 2 * * * cd /opt/ai-ass-core && ./scripts/backup/backup.sh
\`\`\`

### Миграция на новый сервер

**На старом сервере:**
\`\`\`bash
# Создание резервной копии
./scripts/backup/backup.sh

# Упаковка проекта
cd ..
tar -czf ai-stack-migration.tar.gz ai-ass-core/

# Передача на новый сервер
scp ai-stack-migration.tar.gz user@new-server:/opt/
\`\`\`

**На новом сервере:**
\`\`\`bash
# Распаковка
cd /opt
tar -xzf ai-stack-migration.tar.gz
cd ai-ass-core

# Установка зависимостей (Docker, Docker Compose)
# См. docs/installation.md

# Восстановление
./scripts/backup/restore.sh

# Запуск
make up-cpu
\`\`\`

---

## 🔧 Устранение неполадок

### Проверка логов

\`\`\`bash
# Все сервисы
make logs

# Конкретный сервис
docker compose logs -f <service-name>

# Примеры
docker compose logs -f traefik
docker compose logs -f postgres
docker compose logs -f ollama
docker compose logs -f n8n
\`\`\`

### Проблемы с SSL/TLS

\`\`\`bash
# Проверка сертификатов
docker compose logs traefik | grep -i certificate

# Пересоздание сертификатов
docker compose down
rm -rf volumes/traefik-acme/
docker compose up -d traefik

# Проверка DNS
dig @1.1.1.1 n8n.your-domain.com
\`\`\`

### Ollama не отвечает

\`\`\`bash
# Проверка статуса
docker exec -it ollama ollama list

# Проверка логов
docker compose logs ollama

# Перезапуск
docker compose restart ollama

# Тест модели
docker exec -it ollama ollama run llama3.2 "Test"
\`\`\`

### PostgreSQL проблемы

\`\`\`bash
# Проверка подключения
docker exec -it postgres psql -U postgres -c "\\l"

# Проверка схем
docker exec -it postgres psql -U postgres -d postgres -c "\\dn"

# Проверка подключений
docker exec -it postgres psql -U postgres -c "SELECT * FROM pg_stat_activity;"

# Перезапуск
docker compose restart postgres
\`\`\`

### Проблемы с памятью

\`\`\`bash
# Проверка использования
docker stats --no-stream

# Очистка
make clean

# Добавление swap (если < 16GB RAM)
sudo fallocate -l 8G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
\`\`\`

### n8n не запускается

\`\`\`bash
# Проверка БД
docker compose logs postgres

# Проверка Redis
docker compose logs redis

# Очистка и перезапуск
docker compose stop n8n n8n-worker
docker compose rm -f n8n n8n-worker
docker compose up -d n8n n8n-worker
\`\`\`

### Проблемы с дисковым пространством

\`\`\`bash
# Проверка использования
df -h
docker system df

# Очистка неиспользуемых образов
docker image prune -a

# Очистка неиспользуемых volumes
docker volume prune

# Полная очистка (ОСТОРОЖНО!)
make clean-all
\`\`\`

---

## 🔐 Безопасность

### Рекомендации

1. **Используйте надежные пароли**
   - Минимум 16 символов
   - Используйте менеджер паролей
   - Регулярно меняйте пароли

2. **Настройте firewall**
   \`\`\`bash
   # UFW
   sudo ufw allow 22/tcp   # SSH
   sudo ufw allow 80/tcp   # HTTP
   sudo ufw allow 443/tcp  # HTTPS
   sudo ufw enable
   \`\`\`

3. **Включите fail2ban**
   \`\`\`bash
   sudo apt install fail2ban
   sudo systemctl enable fail2ban
   sudo systemctl start fail2ban
   \`\`\`

4. **Регулярно обновляйте**
   \`\`\`bash
   # Обновление образов
   make pull
   make down
   make up-cpu
   
   # Обновление системы
   sudo apt update && sudo apt upgrade -y
   \`\`\`

5. **Мониторинг логов**
   \`\`\`bash
   # Отслеживание подозрительной активности
   sudo tail -f /var/log/auth.log
   docker compose logs -f traefik
   \`\`\`

6. **Резервное копирование**
   \`\`\`bash
   # Автоматическое ежедневное резервное копирование
   crontab -e
   # 0 2 * * * cd /opt/ai-ass-core && ./scripts/backup/backup.sh
   \`\`\`

7. **Ограничение доступа**
   - Используйте VPN для доступа к административным панелям
   - Настройте Traefik BasicAuth для чувствительных сервисов
   - Ограничьте доступ по IP через middleware Traefik

### Cloudflare настройки

1. **Включите SSL/TLS Full (strict)**
2. **Настройте WAF (Web Application Firewall)**
3. **Включите DDoS Protection**
4. **Настройте Rate Limiting**
5. **Используйте Cloudflare Access для дополнительной аутентификации**

---

## 📚 Дополнительные ресурсы

### Документация проекта

- [Инструкция по установке](docs/installation.md)
- [Руководство по конфигурации](docs/configuration.md)
- [Устранение неполадок](docs/troubleshooting.md)
- [Архитектурные решения](docs/architecture/)

### Документация компонентов

- [n8n Documentation](https://docs.n8n.io/)
- [Ollama Documentation](https://github.com/ollama/ollama/blob/main/docs/README.md)
- [Open WebUI Documentation](https://docs.openwebui.com/)
- [Supabase Documentation](https://supabase.com/docs)
- [Langfuse Documentation](https://langfuse.com/docs)
- [Traefik Documentation](https://doc.traefik.io/traefik/)
- [LlamaIndex Documentation](https://docs.llamaindex.ai/)
- [Grafana Documentation](https://grafana.com/docs/)
- [Prometheus Documentation](https://prometheus.io/docs/)

### Сообщество и поддержка

- [GitHub Issues](https://github.com/Bimo0420/ai-ass-core/issues)
- [Discussions](https://github.com/Bimo0420/ai-ass-core/discussions)

---

## 🤝 Участие в разработке

Мы приветствуем вклад в проект! Пожалуйста:

1. Fork репозитория
2. Создайте feature branch (\`git checkout -b feature/AmazingFeature\`)
3. Commit изменений (\`git commit -m 'Add some AmazingFeature'\`)
4. Push в branch (\`git push origin feature/AmazingFeature\`)
5. Откройте Pull Request

### Правила кода

- Следуйте существующему стилю кода
- Добавляйте комментарии к сложной логике
- Обновляйте документацию при необходимости
- Тестируйте изменения перед отправкой PR

---

## 📄 Лицензия

Этот проект использует различные компоненты с разными лицензиями. Проверьте лицензии каждого компонента отдельно.

---

## ✨ Авторы

- **Bimo0420** - *Начальная работа* - [GitHub](https://github.com/Bimo0420)

---

## 🙏 Благодарности

- Всем разработчикам open-source проектов, используемых в этом стеке
- Сообществу за вклад и обратную связь

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел [Устранение неполадок](#-устранение-неполадок)
2. Просмотрите [существующие issues](https://github.com/Bimo0420/ai-ass-core/issues)
3. Создайте [новый issue](https://github.com/Bimo0420/ai-ass-core/issues/new) с подробным описанием

---

<div align="center">

**Сделано с ❤️ для AI-энтузиастов**

⭐ Если проект был полезен, поставьте звезду на GitHub!